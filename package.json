{
  "name": "noname-store-clean-architecture",
  "private": true,
  "workspaces": [
    "apps/*",
    "domain"
  ],
  "version": "1.0.0",
  "description": "",
  
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "test": "yarn workspaces foreach run test",
    "clean": "yarn workspaces foreach run clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "yarn@4.9.2",
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
